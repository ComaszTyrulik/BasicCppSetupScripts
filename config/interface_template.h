#pragma once

#include <Common/Helpers.h>

namespace {namespace}
{{
    class {class_name}
    {{
    public:
        SCE_IMPLEMENT_ADDITIONAL_CONSTRUCTORS_DELETED({class_name});
        {class_name}() = default;
        virtual ~{class_name}() = default;
    }};
}} // namespace {namespace}
